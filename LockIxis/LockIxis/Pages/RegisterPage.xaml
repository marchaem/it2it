<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="LockIxis.Pages.RegisterPage">
  <RelativeLayout>
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="2*" />
        <RowDefinition Height="*" />
        <RowDefinition Height="200" />
      </Grid.RowDefinitions>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>
      <Label Text="LockIxis" VerticalOptions="Center" HorizontalOptions="Center" Grid.Row="0" />
      <Entry x:Name="usernameEntry"
        Text="{Binding Username}"
        Placeholder="Login"
        HeightRequest="45" />
      <Button Text="Scan my Private Key"
              HorizontalOptions="Center"
              VerticalOptions="CenterAndExpand"
              Grid.Row="1" Grid.Column="0"
              Command="{Binding RegisterUserCommand}"
              />
      <Button Text="Quit"
            HorizontalOptions="Center"
            VerticalOptions="CenterAndExpand" Grid.Row="1" Grid.Column="1"  />
    </Grid>
  </RelativeLayout>
</ContentPage>
